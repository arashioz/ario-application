  docker compose --env-file .env.prod up -d --build
  docker compose --env-file .env.prod exec backend node dist/seed.
  docker compose --env-file .env.prod exec backend node dist/seed.js# === آریو — دیپلوی روی IP ===
# IP سرور را جایگزین کن، مثلاً: DOMAIN=203.0.113.10

DOMAIN=YOUR.SERVER.IP
SSL_MODE=selfsigned

CERTBOT_EMAIL=admin@example.com
SHOP_OPENING_DATE=2025-05-27
PORT=3001
WS_PATH=/ws
NODE_ENV=production
